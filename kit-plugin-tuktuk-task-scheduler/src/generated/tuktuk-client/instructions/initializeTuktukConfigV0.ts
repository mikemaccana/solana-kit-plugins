/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findTuktukConfigPda } from "../pdas";
import { TUKTUK_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_TUKTUK_CONFIG_V0_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([67, 128, 98, 227, 103, 60, 179, 214]);

export function getInitializeTuktukConfigV0DiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TUKTUK_CONFIG_V0_DISCRIMINATOR,
  );
}

export type InitializeTuktukConfigV0Instruction<
  TProgram extends string = typeof TUKTUK_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountApprover extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountTuktukConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountApprover extends string
        ? ReadonlySignerAccount<TAccountApprover> &
            AccountSignerMeta<TAccountApprover>
        : TAccountApprover,
      TAccountAuthority extends string
        ? ReadonlyAccount<TAccountAuthority>
        : TAccountAuthority,
      TAccountTuktukConfig extends string
        ? WritableAccount<TAccountTuktukConfig>
        : TAccountTuktukConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTuktukConfigV0InstructionData = {
  discriminator: ReadonlyUint8Array;
  minDeposit: bigint;
};

export type InitializeTuktukConfigV0InstructionDataArgs = {
  minDeposit: number | bigint;
};

export function getInitializeTuktukConfigV0InstructionDataEncoder(): FixedSizeEncoder<InitializeTuktukConfigV0InstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["minDeposit", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_TUKTUK_CONFIG_V0_DISCRIMINATOR,
    }),
  );
}

export function getInitializeTuktukConfigV0InstructionDataDecoder(): FixedSizeDecoder<InitializeTuktukConfigV0InstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["minDeposit", getU64Decoder()],
  ]);
}

export function getInitializeTuktukConfigV0InstructionDataCodec(): FixedSizeCodec<
  InitializeTuktukConfigV0InstructionDataArgs,
  InitializeTuktukConfigV0InstructionData
> {
  return combineCodec(
    getInitializeTuktukConfigV0InstructionDataEncoder(),
    getInitializeTuktukConfigV0InstructionDataDecoder(),
  );
}

export type InitializeTuktukConfigV0AsyncInput<
  TAccountPayer extends string = string,
  TAccountApprover extends string = string,
  TAccountAuthority extends string = string,
  TAccountTuktukConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  approver: TransactionSigner<TAccountApprover>;
  authority: Address<TAccountAuthority>;
  tuktukConfig?: Address<TAccountTuktukConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  minDeposit: InitializeTuktukConfigV0InstructionDataArgs["minDeposit"];
};

export async function getInitializeTuktukConfigV0InstructionAsync<
  TAccountPayer extends string,
  TAccountApprover extends string,
  TAccountAuthority extends string,
  TAccountTuktukConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TUKTUK_PROGRAM_ADDRESS,
>(
  input: InitializeTuktukConfigV0AsyncInput<
    TAccountPayer,
    TAccountApprover,
    TAccountAuthority,
    TAccountTuktukConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeTuktukConfigV0Instruction<
    TProgramAddress,
    TAccountPayer,
    TAccountApprover,
    TAccountAuthority,
    TAccountTuktukConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TUKTUK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    approver: { value: input.approver ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    tuktukConfig: { value: input.tuktukConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tuktukConfig.value) {
    accounts.tuktukConfig.value = await findTuktukConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("approver", accounts.approver),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("tuktukConfig", accounts.tuktukConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTuktukConfigV0InstructionDataEncoder().encode(
      args as InitializeTuktukConfigV0InstructionDataArgs,
    ),
    programAddress,
  } as InitializeTuktukConfigV0Instruction<
    TProgramAddress,
    TAccountPayer,
    TAccountApprover,
    TAccountAuthority,
    TAccountTuktukConfig,
    TAccountSystemProgram
  >);
}

export type InitializeTuktukConfigV0Input<
  TAccountPayer extends string = string,
  TAccountApprover extends string = string,
  TAccountAuthority extends string = string,
  TAccountTuktukConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  approver: TransactionSigner<TAccountApprover>;
  authority: Address<TAccountAuthority>;
  tuktukConfig: Address<TAccountTuktukConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  minDeposit: InitializeTuktukConfigV0InstructionDataArgs["minDeposit"];
};

export function getInitializeTuktukConfigV0Instruction<
  TAccountPayer extends string,
  TAccountApprover extends string,
  TAccountAuthority extends string,
  TAccountTuktukConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TUKTUK_PROGRAM_ADDRESS,
>(
  input: InitializeTuktukConfigV0Input<
    TAccountPayer,
    TAccountApprover,
    TAccountAuthority,
    TAccountTuktukConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeTuktukConfigV0Instruction<
  TProgramAddress,
  TAccountPayer,
  TAccountApprover,
  TAccountAuthority,
  TAccountTuktukConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TUKTUK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    approver: { value: input.approver ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    tuktukConfig: { value: input.tuktukConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("approver", accounts.approver),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("tuktukConfig", accounts.tuktukConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTuktukConfigV0InstructionDataEncoder().encode(
      args as InitializeTuktukConfigV0InstructionDataArgs,
    ),
    programAddress,
  } as InitializeTuktukConfigV0Instruction<
    TProgramAddress,
    TAccountPayer,
    TAccountApprover,
    TAccountAuthority,
    TAccountTuktukConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTuktukConfigV0Instruction<
  TProgram extends string = typeof TUKTUK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    approver: TAccountMetas[1];
    authority: TAccountMetas[2];
    tuktukConfig: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitializeTuktukConfigV0InstructionData;
};

export function parseInitializeTuktukConfigV0Instruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeTuktukConfigV0Instruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      approver: getNextAccount(),
      authority: getNextAccount(),
      tuktukConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTuktukConfigV0InstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
