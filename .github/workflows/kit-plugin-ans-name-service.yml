name: kit-plugin-ans-name-service

# Runs only when this plugin (or its workflow) changes, so each plugin is
# built and tested independently.
on:
  push:
    paths:
      - 'kit-plugin-ans-name-service/**'
      - '.github/workflows/kit-plugin-ans-name-service.yml'
  pull_request:
    paths:
      - 'kit-plugin-ans-name-service/**'
      - '.github/workflows/kit-plugin-ans-name-service.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kit-plugin-ans-name-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: kit-plugin-ans-name-service/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm run test:ci
