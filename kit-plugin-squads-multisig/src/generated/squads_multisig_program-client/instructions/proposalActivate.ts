/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROPOSAL_ACTIVATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([11, 34, 92, 248, 154, 27, 51, 106]);

export function getProposalActivateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSAL_ACTIVATE_DISCRIMINATOR,
  );
}

export type ProposalActivateInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountMember extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string
        ? ReadonlyAccount<TAccountMultisig>
        : TAccountMultisig,
      TAccountMember extends string
        ? WritableSignerAccount<TAccountMember> &
            AccountSignerMeta<TAccountMember>
        : TAccountMember,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      ...TRemainingAccounts,
    ]
  >;

export type ProposalActivateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProposalActivateInstructionDataArgs = {};

export function getProposalActivateInstructionDataEncoder(): FixedSizeEncoder<ProposalActivateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PROPOSAL_ACTIVATE_DISCRIMINATOR }),
  );
}

export function getProposalActivateInstructionDataDecoder(): FixedSizeDecoder<ProposalActivateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProposalActivateInstructionDataCodec(): FixedSizeCodec<
  ProposalActivateInstructionDataArgs,
  ProposalActivateInstructionData
> {
  return combineCodec(
    getProposalActivateInstructionDataEncoder(),
    getProposalActivateInstructionDataDecoder(),
  );
}

export type ProposalActivateInput<
  TAccountMultisig extends string = string,
  TAccountMember extends string = string,
  TAccountProposal extends string = string,
> = {
  multisig: Address<TAccountMultisig>;
  member: TransactionSigner<TAccountMember>;
  proposal: Address<TAccountProposal>;
};

export function getProposalActivateInstruction<
  TAccountMultisig extends string,
  TAccountMember extends string,
  TAccountProposal extends string,
  TProgramAddress extends Address =
    typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProposalActivateInput<
    TAccountMultisig,
    TAccountMember,
    TAccountProposal
  >,
  config?: { programAddress?: TProgramAddress },
): ProposalActivateInstruction<
  TProgramAddress,
  TAccountMultisig,
  TAccountMember,
  TAccountProposal
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisig: { value: input.multisig ?? null, isWritable: false },
    member: { value: input.member ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("member", accounts.member),
      getAccountMeta("proposal", accounts.proposal),
    ],
    data: getProposalActivateInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposalActivateInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountMember,
    TAccountProposal
  >);
}

export type ParsedProposalActivateInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    member: TAccountMetas[1];
    proposal: TAccountMetas[2];
  };
  data: ProposalActivateInstructionData;
};

export function parseProposalActivateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposalActivateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      member: getNextAccount(),
      proposal: getNextAccount(),
    },
    data: getProposalActivateInstructionDataDecoder().decode(instruction.data),
  };
}
