/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import {
  getProposalVoteArgsDecoder,
  getProposalVoteArgsEncoder,
  type ProposalVoteArgs,
  type ProposalVoteArgsArgs,
} from "../types";

export const PROPOSAL_CANCEL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [27, 42, 127, 237, 38, 163, 84, 203],
);

export function getProposalCancelDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSAL_CANCEL_DISCRIMINATOR,
  );
}

export type ProposalCancelInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountMember extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string
        ? ReadonlyAccount<TAccountMultisig>
        : TAccountMultisig,
      TAccountMember extends string
        ? WritableSignerAccount<TAccountMember> &
            AccountSignerMeta<TAccountMember>
        : TAccountMember,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      ...TRemainingAccounts,
    ]
  >;

export type ProposalCancelInstructionData = {
  discriminator: ReadonlyUint8Array;
  args: ProposalVoteArgs;
};

export type ProposalCancelInstructionDataArgs = { args: ProposalVoteArgsArgs };

export function getProposalCancelInstructionDataEncoder(): Encoder<ProposalCancelInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["args", getProposalVoteArgsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSAL_CANCEL_DISCRIMINATOR }),
  );
}

export function getProposalCancelInstructionDataDecoder(): Decoder<ProposalCancelInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["args", getProposalVoteArgsDecoder()],
  ]);
}

export function getProposalCancelInstructionDataCodec(): Codec<
  ProposalCancelInstructionDataArgs,
  ProposalCancelInstructionData
> {
  return combineCodec(
    getProposalCancelInstructionDataEncoder(),
    getProposalCancelInstructionDataDecoder(),
  );
}

export type ProposalCancelInput<
  TAccountMultisig extends string = string,
  TAccountMember extends string = string,
  TAccountProposal extends string = string,
> = {
  multisig: Address<TAccountMultisig>;
  member: TransactionSigner<TAccountMember>;
  proposal: Address<TAccountProposal>;
  args: ProposalCancelInstructionDataArgs["args"];
};

export function getProposalCancelInstruction<
  TAccountMultisig extends string,
  TAccountMember extends string,
  TAccountProposal extends string,
  TProgramAddress extends Address =
    typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProposalCancelInput<
    TAccountMultisig,
    TAccountMember,
    TAccountProposal
  >,
  config?: { programAddress?: TProgramAddress },
): ProposalCancelInstruction<
  TProgramAddress,
  TAccountMultisig,
  TAccountMember,
  TAccountProposal
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisig: { value: input.multisig ?? null, isWritable: false },
    member: { value: input.member ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("member", accounts.member),
      getAccountMeta("proposal", accounts.proposal),
    ],
    data: getProposalCancelInstructionDataEncoder().encode(
      args as ProposalCancelInstructionDataArgs,
    ),
    programAddress,
  } as ProposalCancelInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountMember,
    TAccountProposal
  >);
}

export type ParsedProposalCancelInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    member: TAccountMetas[1];
    proposal: TAccountMetas[2];
  };
  data: ProposalCancelInstructionData;
};

export function parseProposalCancelInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposalCancelInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      member: getNextAccount(),
      proposal: getNextAccount(),
    },
    data: getProposalCancelInstructionDataDecoder().decode(instruction.data),
  };
}
